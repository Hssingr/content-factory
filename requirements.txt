# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1

# Database
sqlalchemy==2.0.36
alembic==1.14.0
# alembic's own transitive dependency (its migration-file template engine) —
# not previously pinned here, which left `alembic` uninstallable/unusable in
# any environment that didn't happen to already have it (surfaced by Agent 6
# roadmap Phase B/C's migration verification, code_report/agent6_metadata_roadmap.md).
mako==1.3.12
psycopg2-binary==2.9.10

# Queue / Workers
celery[redis]==5.4.0
redis==5.2.0

# Scheduler
apscheduler==3.10.4

# Settings / Config
python-dotenv==1.0.1
pydantic-settings==2.6.1
pydantic==2.10.2

# Encryption
cryptography==44.0.0

# Claude API
anthropic==0.40.0

# HTTP
httpx==0.28.0
aiofiles==24.1.0

# Telegram
python-telegram-bot==21.7

# Image processing
Pillow==11.0.0

# Image generation
fal-client==1.0.0

# Audio / Video utilities
cartesia==1.3.1
elevenlabs==2.51.0
openai==2.41.0
# mp3 chunk stitching uses ffmpeg directly (pydub removed — audioop dropped in Python 3.13)
num2words==0.5.14      # digit → spoken-form expansion for Whisper phrase matching
faster-whisper==1.1.1  # local Whisper fallback (CPU, int8) when OpenAI API is down

# Auth
PyJWT==2.13.0

# Utilities
python-multipart==0.0.12
